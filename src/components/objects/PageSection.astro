---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"section"> {
  fullWidth?: boolean;
}

const props: Props = Astro.props;
---

<section class:list={["px-5 lg:px-16", props.class]}>
  <slot />
</section>
